# Small AI Assistant

Repository layout:

- `app/`: macOS RightKey desktop app (Swift package).
- `website/`: marketing and checkout website.

Desktop app quick start:

```bash
cd app
swift test
```

Planning source of truth:
- `app/ROADMAP.md` (loadstar roadmap)
